import pandas as pd 
import csv 
import requests 
from matplotlib import pyplot as plt
import excel  

path_to_file = "C:\\Users\\Uncle Namo\\Desktop\\ds_salaries.csv"
ds_salaries = pd.read_csv(path_to_file)
#print(ds_salaries)

#Columns 
print(ds_salaries.columns)

#Print Dimensions 
print(ds_salaries.shape)

#Print the data types 
print(ds_salaries.dtypes)

#Calling Data In Our Dataset
print(ds_salaries['salary'])
print(ds_salaries['work_year'])


plt.bar(ds_salaries['experience_level'],ds_salaries['salary_in_usd'], color = 'green')

#Add titles and axis names 
plt.title("Salaries in USD based on Position")
plt.ylabel("USD", fontsize = 8)


plt.show()




